name: Build Python Applications with Nuitka

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      # Check-out repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Setup Python 3.12
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: 'x64'
          cache: 'pip'
          cache-dependency-path: buildv2.txt

      # Install base dependencies from buildv2.txt
      - name: Install base dependencies
        run: |
          pip install -r buildv2.txt

      # Build Client Application (không thay đổi)
      - name: Build Client Application
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: Client/main.py
          mode: standalone
          enable-plugins: |
            pyside6
          assume-yes-for-downloads: true
          disable-console: ${{ runner.os == 'Windows' }}
          output-dir: build/Client App

      # Build Monitoring Unit Application (macOS: phiên bản cơ bản)
      - name: Build Monitoring Unit Application (Basic - macOS)
        if: matrix.os == 'macos-latest'  # Chỉ chạy trên macOS
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: Monitoring Unit/main.py
          mode: standalone
          assume-yes-for-downloads: true
          output-dir: build/Monitoring Unit App

      # Install PyTorch with CUDA 12.4 (Windows và Linux)
      - name: Install PyTorch with CUDA 12.4
        if: matrix.os != 'macos-latest' 
        run: |
          pip uninstall -y torch torchvision torchaudio
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

      # Build Monitoring Unit Application with CUDA 12.4 (Windows và Linux)
      - name: Build Monitoring Unit Application (CUDA 12.4)
        if: matrix.os != 'macos-latest'  
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: Monitoring Unit/main.py
          mode: standalone
          assume-yes-for-downloads: true
          disable-console: ${{ runner.os == 'Windows' }}
          output-dir: build/Monitoring Unit App cu124

      # Install PyTorch with CUDA 12.6 (Windows và Linux)
      - name: Install PyTorch with CUDA 12.6
        if: matrix.os != 'macos-latest'  
        run: |
          pip uninstall -y torch torchvision torchaudio
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

      # Build Monitoring Unit Application with CUDA 12.6 (Windows và Linux)
      - name: Build Monitoring Unit Application (CUDA 12.6)
        if: matrix.os != 'macos-latest'  
        uses: Nuitka/Nuitka-Action@main
        with:
          nuitka-version: main
          script-name: Monitoring Unit/main.py
          mode: standalone
          assume-yes-for-downloads: true
          disable-console: ${{ runner.os == 'Windows' }}
          output-dir: build/Monitoring Unit App cu126

      # Upload Client Artifact
      - name: Upload Client Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-Client-App
          path: |
            build/Client App/**
          include-hidden-files: true

      # Upload Monitoring Unit Artifact (Basic - macOS)
      - name: Upload Monitoring Unit Artifact (Basic - macOS)
        if: matrix.os == 'macos-latest' 
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-Monitoring-Unit-App
          path: |
            build/Monitoring Unit App/**
          include-hidden-files: true

      # Upload Monitoring Unit Artifact (CUDA 12.4 - Windows và Linux)
      - name: Upload Monitoring Unit Artifact (CUDA 12.4)
        if: matrix.os != 'macos-latest'  
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-Monitoring-Unit-App-cu124
          path: |
            build/Monitoring Unit App cu124/**
          include-hidden-files: true

      # Upload Monitoring Unit Artifact (CUDA 12.6 - Windows và Linux)
      - name: Upload Monitoring Unit Artifact (CUDA 12.6)
        if: matrix.os != 'macos-latest'  # Không chạy trên macOS
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-Monitoring-Unit-App-cu126
          path: |
            build/Monitoring Unit App cu126/**
          include-hidden-files: true